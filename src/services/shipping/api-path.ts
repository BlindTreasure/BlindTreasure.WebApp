const SHIPPING = "/shipping";
const SHIPPING_PROVINCES = `${SHIPPING}/provinces`;
const SHIPPING_DISTRICTS = `${SHIPPING}/districts`;
const SHIPPING_WARDS = `${SHIPPING}/wards`;

export default {
  SHIPPING,
  SHIPPING_PROVINCES,
  SHIPPING_DISTRICTS,
  SHIPPING_WARDS,
};
